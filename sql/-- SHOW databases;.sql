
-- SQLテーブル作成等保存

-- SHOW databases;
-- USE riceball;
-- CREATE TABLE items (
--   id integer primary key NOT NULL,
--   name text NOT NULL,
--   price integer NOT NULL
-- );
-- INSERT INTO items (id, name, price) VALUES (1, '塩おにぎり', 130);
-- INSERT INTO items VALUES (2, '鮭おにぎり', 200);
-- INSERT INTO items VALUES (3, '昆布おにぎり', 170);
-- INSERT INTO items VALUES (4, '明太子おにぎり', 190),
--   (5, '五目おにぎり', 160),
--   (6, '赤飯おにぎり', 190),
--   (7, 'たらこおにぎり', 190),
--   (8, '高菜おにぎり', 160),
--   (9, '天むす', 220);

-- CREATE TABLE contact (
--   id integer auto_increment,
--   name: text NOT NULL,
--   email: text NOT NULL,
--   message: text NOT NULL
-- );


-- SELECT * FROM items


-- SHOW databases;
-- USE riceball;
-- DROP TABLE contacts;
-- CREATE TABLE contacts (
--   id integer auto_increment primary key NOT NULL,
--   name text NOT NULL,
--   email text NOT NULL,
--   message text NOT NULL
-- );
-- INSERT INTO contacts (name, email, message)
-- VALUES ('Max', 'max@example.com', 'good');
-- SELECT *
-- from contacts;

-- SHOW columns FROM contacts;
